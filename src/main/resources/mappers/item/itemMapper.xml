<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.shopmall.itemMapper">
	<!-- 상품리스트 -->					
	<select id="mainListItem" resultType="itemVO">
		SELECT 
			item_no, 
			item_image,
			item_name,
			item_price
		FROM
			items
		ORDER BY item_name DESC
	</select>
	
	<!-- 상품리스트 -->					
	<select id="cateList" resultType="itemVO">
		SELECT 
			item_no, 
			item_image,
			item_name,
			item_price
		FROM
			items
		ORDER BY item_name DESC
	</select>
	
	<!-- 총 아이템 갯수 출력 -->
	<select id="countItem" resultType="int">
		SELECT COUNT(*) FROM items
	</select>

	<!-- 페이징 처리 후 아이템 조회 -->
	<select id="selectItem" resultType="itemVO" parameterType="com.study.shopmall.general.utils.PagingVO">
		SELECT @ROWNUM:=@ROWNUM+1, item_no, item_name, item_price, item_image
		FROM   items
		WHERE  (@ROWNUM:=#{start})=#{end}   
	</select>
</mapper>
  